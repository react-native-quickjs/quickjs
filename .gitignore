# macOS
.DS_Store

# Editors
.vscode/
.idea

# node
node_modules/
npm-debug.log

# Xcode
build/
DerivedData
xcuserdata
*.xcuserstate
project.xcworkspace
**/.xcode.env.local

# Android
.cxx
.gradle
local.properties

# CocoaPods and Ruby (example app)
example/ios/Pods
example/vendor/

# Expo
.expo/

# Out-of-source CMake build directories (build-rel, build-napi, build-intl).
# Directories only -- the trailing slash is load-bearing, or the pattern also
# swallows build-*.sh and build-*.mjs scripts that are meant to be tracked.
build-*/

# Debug symbol bundles
*.dSYM/

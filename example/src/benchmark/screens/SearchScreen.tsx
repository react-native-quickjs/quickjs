import React, { useMemo, useState } from 'react'; import { ScrollView, TextInput } from 'react-native'; import { feed } from '../data'; import { FeedRow, ScreenFrame, styles } from '../components'; import { searchItems } from '../utils';
export default function SearchScreen({ open }: { open: (route: string) => void }) { const [query, setQuery] = useState(''); const results = useMemo(() => searchItems(feed, query), [query]); return <ScreenFrame title="Search"><TextInput value={query} onChangeText={setQuery} placeholder="Search your workspace" style={styles.search} /><ScrollView>{results.map(item => <FeedRow key={item.id} item={item} onPress={() => open('article')} />)}</ScrollView></ScreenFrame>; }

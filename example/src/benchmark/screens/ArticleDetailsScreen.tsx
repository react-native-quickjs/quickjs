import React from 'react'; import { ScrollView, Text } from 'react-native'; import { feed } from '../data'; import { ScreenFrame, styles } from '../components';
export default function ArticleDetailsScreen({ back }: { back: () => void }) { const item = feed[0]; return <ScreenFrame title={item.category} onBack={back}><ScrollView><Text style={styles.heroTitle}>{item.title}</Text><Text style={styles.muted}>{item.author} · {item.minutes} min read</Text><Text style={styles.body}>Good products make the next useful action obvious. This locally stored article demonstrates detail rendering, formatting, and a stable reading state without network work.</Text><Text style={styles.body}>The same content is available to every engine and remains untouched until this screen is opened.</Text></ScrollView></ScreenFrame>; }

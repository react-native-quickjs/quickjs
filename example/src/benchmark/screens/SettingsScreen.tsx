import React from 'react'; import { Pressable, ScrollView, Text } from 'react-native'; import { ScreenFrame, styles } from '../components';
export default function SettingsScreen({ open }: { open: (route: string) => void }) { const items: [string, string][] = [['Account', 'editProfile'], ['Notifications', 'notifications'], ['Privacy', 'security'], ['Appearance', 'preferences'], ['Offline storage', 'saved']]; return <ScreenFrame title="Settings"><ScrollView>{items.map(([label, route]) => <Pressable key={label} onPress={() => open(route)}><Text style={styles.rowTitle}>{label} ›</Text></Pressable>)}</ScrollView></ScreenFrame>; }

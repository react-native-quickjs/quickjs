import React from 'react'; import { Pressable, ScrollView, Text } from 'react-native'; import { ScreenFrame, styles } from '../components';
export default function OrdersScreen({ open }: { open: (route: string) => void }) { return <ScreenFrame title="Orders"><ScrollView>{['March workspace plan', 'Design review package', 'Team seats renewal'].map((x, i) => <Pressable key={x} onPress={() => open('orderDetails')}><Text style={styles.rowTitle}>{x}{'\n'}<Text style={styles.muted}>{i === 0 ? 'Complete' : 'Processing'} · local record</Text></Text></Pressable>)}</ScrollView></ScreenFrame>; }

import React from 'react'; import { Pressable, ScrollView, Text } from 'react-native'; import { accounts } from '../data'; import { ScreenFrame, styles } from '../components';
export default function ProfileScreen({ open }: { open: (route: string) => void }) { const person = accounts[0]; return <ScreenFrame title="Profile"><ScrollView><Text style={styles.heroTitle}>{person.name}</Text><Text style={styles.body}>{person.role} · 24 contributions · 8 saved collections</Text><Pressable style={styles.button} onPress={() => open('editProfile')}><Text style={styles.buttonText}>Edit profile</Text></Pressable><Text style={styles.section}>About your workspace</Text><Text style={styles.body}>Your profile brings together preferences, activity summaries, and collaboration settings.</Text></ScrollView></ScreenFrame>; }

import React, { useState } from 'react'; import { ScrollView, Text, TextInput } from 'react-native'; import { ScreenFrame, styles } from '../components'; import { validateProfile } from '../utils';
export default function EditProfileScreen() { const [name, setName] = useState('Alex Morgan'); const [email, setEmail] = useState('alex@example.com'); const valid = validateProfile(name, email); return <ScreenFrame title="Edit profile"><ScrollView><TextInput value={name} onChangeText={setName} style={styles.field} /><TextInput value={email} onChangeText={setEmail} style={styles.field} keyboardType="email-address" /><Text style={valid ? styles.badge : styles.body}>{valid ? 'All changes are valid' : 'Check your details'}</Text></ScrollView></ScreenFrame>; }

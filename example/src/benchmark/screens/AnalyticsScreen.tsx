import React from 'react'; import { ScrollView, Text, View } from 'react-native'; import { feed } from '../data'; import { MetricCard, ScreenFrame, SectionTitle, styles } from '../components';
export default function AnalyticsScreen() { const total = feed.reduce((sum, item) => sum + item.minutes, 0); return <ScreenFrame title="Analytics"><ScrollView><View style={styles.metrics}><MetricCard label="Items read" value="12" hint="this month" /><MetricCard label="Minutes" value={String(total)} hint="in your feed" /></View><SectionTitle>By category</SectionTitle>{Array.from(new Set(feed.map(item => item.category))).map(category => <Text key={category} style={styles.rowTitle}>{category} · {feed.filter(item => item.category === category).length} items</Text>)}</ScrollView></ScreenFrame>; }

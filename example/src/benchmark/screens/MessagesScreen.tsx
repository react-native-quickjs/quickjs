import React from 'react'; import { Pressable, ScrollView, Text } from 'react-native'; import { accounts } from '../data'; import { ScreenFrame, styles } from '../components';
export default function MessagesScreen({ open }: { open: (route: string) => void }) { return <ScreenFrame title="Messages"><ScrollView>{accounts.map(account => <Pressable key={account.id} onPress={() => open('messageDetails')}><Text style={styles.rowTitle}>{account.initials}  {account.name}{'\n'}<Text style={styles.muted}>Latest project update is ready to review</Text></Text></Pressable>)}</ScrollView></ScreenFrame>; }

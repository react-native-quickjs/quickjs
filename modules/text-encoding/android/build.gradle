buildscript {
  repositories { google(); mavenCentral() }
  dependencies {
    classpath("com.android.tools.build:gradle:8.7.2")
    classpath("org.jetbrains.kotlin:kotlin-gradle-plugin:2.0.21")
  }
}

apply plugin: "com.android.library"
apply plugin: "org.jetbrains.kotlin.android"

// This package ships no native code of its own. The module core and the Android
// platform JNI are compiled into the engine's single .so by the
// @react-native-quickjs/quickjs package (which discovers this module through
// its reactNativeQuickJSModule field). This library is the Kotlin half only.

android {
  namespace "com.text_encoding"
  compileSdk 35

  defaultConfig {
    minSdk 24
  }

  sourceSets {
    main {
      java.srcDirs = ["src/main/java"]
      manifest.srcFile "src/main/AndroidManifest.xml"
    }
  }
}

dependencies {
  implementation "com.facebook.react:react-android"
  implementation "org.jetbrains.kotlin:kotlin-stdlib"
}
